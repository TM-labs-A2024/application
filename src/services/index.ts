import { useDoctorMutation } from './doctors'
import { useInstitutionMutation, useInstitutions } from './institutions'
import { useNurseMutation } from './nurses'
import { usePatientMutation } from './patients'
import { useSpecialties } from './specialties'

export {
  useInstitutionMutation,
  useDoctorMutation,
  useNurseMutation,
  usePatientMutation,
  useInstitutions,
  useSpecialties
}
